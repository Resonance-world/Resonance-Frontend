import { ThemeSelection } from '@/components/home/ThemeSelection';

/**
 * Theme Selection Page
 * Displays all available prompt themes for user selection
 */
export default function ThemesPage() {
  return <ThemeSelection />;
} 