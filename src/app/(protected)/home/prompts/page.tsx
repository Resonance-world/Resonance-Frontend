import { PromptSelection } from '@/components/home/PromptSelection';

/**
 * Prompt Selection Page
 * Displays predefined prompts for selected theme and custom prompt option
 */
export default function PromptsPage() {
  return <PromptSelection />;
} 